# Archive

This directory contains older prototypes and historical experiments.

Files here are preserved for traceability but are not part of the current primary TAIS API.

Current primary code lives in:

- `tais_core/`
- `tais_swarm_v6/`
- `experiments/`
- `tests/`
